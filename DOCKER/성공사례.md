1. 
```
FROM ubuntu:latest

RUN apt-get update

ADD entrypoint.sh /entrypoint.sh

RUN chmod 777 /entrypoint.sh

ENTRYPOINT ["/bin/bash"]

-> ADD image 에 포함시킬 것, chmod는 ubuntu가 설치되었기 때문에 가능한 것 같음
```


2. 
```
FROM python:3.8.1

ADD test.py /test.py

RUN chmod 777 /test.py

CMD ["/bin/bash"]
-> RUN치니까 바로 BASH 들어가짐, 근데 ["bin/bash","python test.py"] 는 안 됨.. 

엔트리 포인트를 python3 으로 했기 때문에..

더불어 workdir이 PECNETT이니까

맨 뒤에 나는 인자를 

/scipts/test_pretrained_model.py -lf PECNET_social_model1.py

```


3. 
```
FROM python:3.8.1

WORKDIR /PECNett

ADD PECNett /PECNett

RUN pip install -r requirements.txt -> 이거 설치를 위해 위에 workdir을 PECNett으로 한 것임

ARG file_to_load

WORKDIR /PECNett/scripts - > python파일이 /scripts/..에 있어서 /scripts/~.py 하고 줘야하는데 오류가 떠서 이렇게 바꿈. 그랬더니 되버림.

ENTRYPOINT ["python3"] - > 컨테이너 만들 때 자동 실행
```