[중계기 proxy server](#proxy)

[방화벽 fire wall](#방화벽)
- RTMP : Real Time Messaging Protocol, 컴퓨터 통신 규약, http랑 비슷한 개념인긴한데 "인터넷을 통한 오디오, 비디오, 데이터를 실시간으로 스트리밍할 때의 사용 하는 프로토콜", 프로토콜 말고 모듈이라는 개념으로도 부르는 듯(희완쌤)
    - RTMP "CLIENT" 소프트웨어
        클라이언트 임으로 , 서버로 진입할 때 사용하는 SW임
        1. 어도비 플래시 미디어 서버
        2. WebORB 통합서버(무료.NET, JAVA - ColdFuSion)
        3. NGINX 기반 RTMP 미디어 스트리밍 서버(?), 써버라고 ?
    - RTMP 예시
        1. YOUTUBE의 실시간 스트리밍 
    

### proxy
- Proxy Server
    - 설명
    ```
    forward proxy
    cline와 메인 server 사이에서 데이터를 전달해주는 서버
    흠.. 즉 클라와 메인서버 사이에 또 서버를 두었다는거네.
    즉 클라와 메인서버 사이에 제 3자가 껴가지고 직접적인 연결을 해주지 않는 다는거네... 즉 여러개의 서버가 중간에 낄 수 있는거네 ?
    
    위키에서도는 많은 사람들이 내 ip 세탁해서 우회용 도구로 알 고 있는데 물론 이것도 있겠지 그런데 첫 목적은 웹 서핑 비롯한 인터넷 속도 향상이 목적이었음.
    내부 정보보안이 필요한 다수 기업에서도 proxy server를 이용해 외부의 internet에 접속함.
    전문용어로 "트롤링" 이라는 것이 있는데 : 내가 아는 그 트롤링같은 분탕질인가 ? ip세척도구로 애용되는데 즉 분탕치려고 내 ip숨기는 용도로 사용한다는 것 같은데 ?
    ```
    - Reverse proxy
        ```
        Porxy server의 일종
        이게 진짜 중간 매개 역할을 하는 서버의 형태인 듯
        클라이언트를 대신해 "한 대 이상의 서버로부터 차원을 추출하는 proxy server"
        메인 서버의 자원들을 마치 "내가 메인 서버다"라는 것처럼 클라이언트가 요청하면 자원들을 proxy 서버의 명세로 클라이언트에게 보내주는 형태.
        ```
        - 대표 tool
            1. 아파치
            2. Ngix
            3. squid
### 방화벽
- 방화벽이 필요한 이유
    ```
    D-DOS 등등 침투하려는 얘들이 너무 많음.
    알게 모르게 
    ```